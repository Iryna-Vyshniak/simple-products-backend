const isValidId = require('./isValidId');
const checkAuth = require('./check-auth');
const upload = require('./upload');
const uploadCloud = require('./uploadCloud');

module.exports = { isValidId, checkAuth, upload, uploadCloud };
